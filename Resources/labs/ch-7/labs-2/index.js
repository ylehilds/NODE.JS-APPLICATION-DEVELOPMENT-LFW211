'use strict'

// via terminal commands:
// command used in terminal: node -p "require('../labs-1/index')(19,23)"
// or
// node -e "console.log(require('../labs-1/index')(19,23))"
// they should both print out the sum of 42

// via code:
const add = require("../labs-1")
console.log(add(19, 23))
